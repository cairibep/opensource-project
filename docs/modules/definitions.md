# Definitions.py

Este módulo contém **constantes e definições auxiliares** usadas em várias partes do compilador.

## Conteúdo típico
- Palavras-chave da linguagem C.
- Operadores e símbolos aceitos.
- Tabelas de mapeamento usadas no parser e tokenizer.

## Exemplo
- Lista de operadores: `+`, `-`, `*`, `/`, `=`, `==`, `<`, `>`  
- Tipos básicos: `int`, `char`, `void`
```

---